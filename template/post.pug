doctype html
html(lang='fr')
    include head
    body
        .wrapper
            include header

            main
                div
                    a(
                        class='retour'
                        href= pathBase + 'index.html'
                    ) Retour accueil

                <h1>#{title}</h1> !{content}