include mixins

doctype html
html(lang='fr')
    include head
    body
        .wrapper
            include header

            main
                div
                    a(
                        class='retour'
                        href= pathBase + 'index.html'
                    ) Retour accueil

                each val in posts
                    +post(val)